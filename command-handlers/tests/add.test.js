describe('add', () => {
  it('should verify the existence of a config file');
  it('should prompt the user for a new service to add');
  it('should write the initial service config to kuda.json');
  it(`should write the initial settings to the services package.json`);
});
